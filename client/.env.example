# Browser calls stay same-origin so Vercel preview domains can use host-only session cookies.
NEXT_PUBLIC_API_URL=/api
# Used by the rewrite proxy and Next.js server rendering.
CLEANOPS_API_INTERNAL_URL=http://localhost:3000/api
