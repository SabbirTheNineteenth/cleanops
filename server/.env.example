PORT=3000
WEB_ORIGIN=http://localhost:3001
# Optional additional exact local/staging web origins. Never use wildcards.
WEB_ORIGINS=
SESSION_COOKIE_DOMAIN=
DATABASE_URL=file:cleanops.db
DATABASE_AUTH_TOKEN=
JWT_SECRET=
# Set true only when every request reaches this service through a trusted proxy.
TRUST_PROXY_HEADERS=false
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
